import PropTypes from 'prop-types'
import styled from 'styled-components'

const StyChipProjects = styled.div`

`

const ChipProjects = () => (
  <div>Chip Projects</div>
)

export default ChipProjects